# Fileserver for site matissetec.dev
## main things
- based for a linux setup
- requires python, flask, flask_cors, sqlite3
- database must already be created
- need to separate fileservice from database service
